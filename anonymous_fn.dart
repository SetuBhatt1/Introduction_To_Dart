void main(){
  const cities = ['goa', 'gujarat', 'rajasthan'];
  cities.map((city)=>city.toUpperCase()).forEach((city)=> print(city));
}

/*

  Where they are used?
  -> One Time Use
  -> Define small and simple function on the fly without giving them name.

*/